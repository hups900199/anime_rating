<div id="intro" class="p-5 text-center">
  <h1 class="mb-0">Users</h1>
</div>

<%= paginate @users %>

<div class="container">
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.name, user %></li>
  <% end %>
</ul>
</div>